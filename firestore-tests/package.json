{
  "name": "s8ll-firestore-rules-tests",
  "description": "Security-rules test suite for firestore.rules, run against the Firestore emulator",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "jest",
    "test:emulator": "cd .. && firebase emulators:exec --project demo-s8ll-rules-test --only firestore \"npm --prefix firestore-tests test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/jest": "^29.5.14",
    "firebase-tools": "^13.23.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
