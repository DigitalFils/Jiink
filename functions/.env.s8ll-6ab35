# Not a secret — see stripeClient.ts. Declares this project as a test-mode
# environment; getStripeClient() refuses to start if the STRIPE_SECRET_KEY
# secret doesn't actually start with sk_test_, so a live key can never end
# up active here by accident.
STRIPE_MODE=test
