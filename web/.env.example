# Prisma resolves a relative SQLite path against the schema's own directory,
# so this points at <project root>/db/custom.db from wherever the repo is
# checked out. It was an absolute path into one machine's home directory,
# which meant the app only ran on that machine.
DATABASE_URL=file:../db/custom.db
